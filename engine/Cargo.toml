[package]
name = "kf_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "kf_engine"
crate-type = ["rlib", "cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
