# 设计细节

[← 回 README](README.md)

这里是完整的技术记录：智能体怎么规划、每个机制在修什么问题、
以及移植正确性是怎么验证的。

---

## 规划循环

```
每次重规划 → 9 条持续无火控制
           + 9 条「首帧静止开火 → 次帧起采用不同无火控制」
           → 各自在沙盒里前推 36 帧
           → 打分（爬密度梯队 / 沿引导梯度接近 / 对准 / 风险 / 开火质量）
           → 选最优首帧动作
```

18 条轨迹最终仍对应 10 个真实首帧动作：9 个无火控制，以及一个静止开火。
9 条开火轨迹的第一帧完全相同，区别只在预测的后续移动；规划器保留其中得分最高的
continuation，并在真实世界只执行首帧开火，下一帧根据新状态重新规划。这避免了旧方案
把「开一枪」错误估计成「开枪后原地站满 36 帧」。

沙盒只给智能体玩家能看到的信息：随机数流重播种，对手控制器重建，
避免其内部目标栈跨帧泄漏。

**开火与移动统一比较**：弹道检查负责评价这一枪的质量，但不再无条件覆盖正在进行的
移动或躲避。规划器同时考虑开枪本身和开枪后的最佳移动 continuation；命中机会、来袭
风险与后续射击位进展最终落在同一个 score 上。弹道检查发现狭窄射击窗口时会取消当前
动作锁定并触发一次完整重规划，但最终仍可能因为躲避或生存分更高而选择不开火。若预计
命中落在 36 帧 rollout 之外，开火质量按 `1800 - 12 × 预计飞行帧数` 计分；若 rollout
内已经真实击杀，则直接使用终局时间分，不重复叠加这项预测奖励。

外层四个手写机制，每个都在修一类朴素逐帧重规划的具体崩坏：

| 机制 | 修的问题 |
|---|---|
| 动作锁定 | 候选分数接近时反复横跳，锁几帧让它走直线 |
| 自弹规避 | 旧计划会让坦克撞上自己刚开的那枪 |
| 卡墙检测 | 指令下了却没位移，就惩罚这组油门/转向 |
| 击杀后求生 | 击杀后 3 秒停止追击，改为专门评估躲弹空间 |

另有一条**猎杀链**奖励：在滚动 3 秒窗口内，每进入一个引导场严格更高的新格子，
基础奖励翻倍（1、2、4 … 上限 64）。基础奖励还乘以随本回合时间单调上升的有界函数
`1 + 7 × (1 - exp(-t/250))`：开局倍率为 1，10 秒约 5.42，20 秒约 7.05，极限小于 8。
它刻意难刷——五道闸门同时成立才算，且以（目标格, 当前格）为键，来回蹭同一条边界
只付一次。

**弹夹压力**按 5 个在途子弹槽计算。储备效用为
`-450 × log(6 / (剩余槽+1))`，所以满槽为 0，槽越少分数越低，而且失去最后一槽比失去
第五槽更昂贵。预计命中的飞行时间权重也会随稀缺度从 `12` 最多提高到 `30`；因此满弹
时保持原有进攻性，低弹时更偏好短而确定的射击。真实终局使用击杀时恢复后的剩余槽，
不会把已经命中的杀伤弹继续算作占用。

---

## 性能：为什么能跑在主线程

| | 参考实现 (Python) | 本实现 (JS) |
|---|---|---|
| 单次规划 p95 | 96 ms | **4.7 ms** |
| 规划位置 | 后台 worker，容忍 6 帧陈旧 | 主线程同步，永不陈旧 |
| 无头吞吐 | — | 22 万帧/秒（≈ 25x，8800x 实时） |

两个来源：

1. 引擎步进（而非射线追踪）才是规划的耗时大头，JS 在这里比 Python 快约 25 倍
2. 采样射击位复用了碰撞系统已有的**分桶墙体索引**，
   相比每次采样扫描全部墙体是渐进复杂度上的改进，不只是常数优化

射线数在 UI 里可调；降到 256 条可把场构建从 18 ms 压到 1 ms。

---

## 验证

**机械与规划回归** — 当前 65 条断言覆盖核心移植机制和后续行为改进：
子弹速度与存活期、5 发上限、墙体包容性、分桶索引与 4000 个随机点的暴力碰撞一致、
回合收尾时间线、fire-continuation 轨迹，以及贴墙摩擦与正面防穿墙。

```bash
node --input-type=module -e "import('./test/suite.js').then(m => console.log(m.summarise(m.runSuite())))"
```

（也可直接在浏览器打开 `test/port.test.html`。当前 65/65 通过。）

但机械断言抓不到"移植歪了的 AI"——它照样守规则，只是打得不对。
所以另有 `test/benchmark.js` 跑完整回合，比对结果的**性状**：

密度场本身是**逐位相同的**：同一张迷宫下，两边每个格子的票数、
梯队划分完全一致，引导包络绝对误差 0.0。

对阵脚本 AI，本实现 150 回合 / 参考 45 回合：

| | 回合数 | 胜率 | 未决 |
|---|---|---|---|
| 参考实现 | 45 | 91.1% | 1 |
| 本实现 | 150 | 85.3% | 12 |

差异不显著（双比例 z ≈ 1.0），本地五个互斥 30 回合区块的种子间波动是 76.7%–90.0%，
回合时长在不同种子段上*两个方向*都会偏，属于迷宫抽样噪声而非系统性差距。
改为 fire-continuation 规划之前，强制开火频次两边完全相同（10 个配对回合各 41 次），
每回合射击数 4.1 vs 4.6。该数字保留为移植阶段的历史对照，不再描述当前决策流水线。

> 说明：参考智能体并没有官方胜率可对标。流传的 98% 属于另一个智能体。
> 所以上表是**对照**，不是及格线。

---

## 引擎里几个不起眼但要命的细节

固定 25 FPS，全程不用 delta-time。

- **坦克贴墙接触** — 每帧的 5 个移动子步各自删除朝墙的法向位移，把剩余位移投影到墙面
  切线；参考尺寸下接触位置误差最多约 `0.8 px`，不需要在每条 MPC rollout 内反复 sweep。
  切向保留率按入射角在 `0.70–0.96` 间变化，并以每帧最多 `2°` 的接触转矩让车身逐渐顺着
  墙，而不是保持原角度僵硬横移。正面碰撞与内角仍完全停止。MPC sandbox 与真实游戏共用
  同一套物理，因此规划会自动看到相同的滑墙结果
- **子弹要反弹过才能打死自己** — 没碰过墙之前对射手无害，所以顺着自己刚开的那枪
  往前开是安全的；一旦擦到墙，它对所有人都致命，包括射手。
  这不是为了绕开枪口重叠而打的补丁，是这个游戏本来的规则
- **回合不在击杀瞬间结束** — 之后世界继续跑 75 帧（3 秒），
  飞行中的子弹仍可能带走幸存者；双杀判定为无人得分
- **格子尺寸每回合都变** — 由迷宫维度推导，渲染和物理都不许假设固定网格间距

---

## 源码结构

```
src/game.js          引擎：坦克、子弹、回合状态机（固定 25 FPS）
src/maze.js          迷宫生成、连通性、距离场、分桶墙体索引
src/killfield/
  teacher.js         ★ 搜索智能体主体：滚动时域控制 + 上述四个机制
  field.js           反向射线 → 射击位密度场（导航 + 瞄准先验）
  sandbox.js         隔离前推环境：重播种随机流、重建对手控制器
  score.js           候选动作打分（死亡 -12000，短路一切）
  chain.js           猎杀链奖励
  risk.js            来袭火力风险：把子弹按反射折线前推的几何估计
src/laika.js         对照用的脚本 AI 对手（三阶段 + LIFO 动作栈）
test/                56 条断言 + 行为基准
```
